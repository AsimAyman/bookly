import 'package:flutter/material.dart';

class MyAdsBody extends StatelessWidget {
  const MyAdsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("MyAdsBody"),
    );
  }
}
